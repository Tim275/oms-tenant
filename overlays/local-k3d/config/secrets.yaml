# =============================================================================
# Local K3D Development Secrets - Credentials Reference (Spicker)
# =============================================================================
# ENTERPRISE PATTERN:
# - stringData format for readability (NOT base64 encoded)
# - Local development only - NOT for production!
# - Credentials match StatefulSet service defaults
# =============================================================================

---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-app-user
  namespace: oms
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: oms
    environment: local-k3d
type: Opaque
stringData:
  # PostgreSQL Development Credentials
  username: "stock"
  password: "stock123"

---
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-default-user
  namespace: oms
  labels:
    app.kubernetes.io/name: rabbitmq
    app.kubernetes.io/part-of: oms
    environment: local-k3d
type: Opaque
stringData:
  # RabbitMQ Development Credentials (default guest user)
  username: "guest"
  password: "guest"
  host: "rabbitmq.oms.svc.cluster.local"
  port: "5672"

---
apiVersion: v1
kind: Secret
metadata:
  name: internal-mongodb-oms-users
  namespace: oms
  labels:
    app.kubernetes.io/name: mongodb
    app.kubernetes.io/part-of: oms
    environment: local-k3d
type: Opaque
stringData:
  # MongoDB Development Credentials (Percona Operator naming convention)
  MONGODB_DATABASE_ADMIN_USER: "admin"
  MONGODB_DATABASE_ADMIN_PASSWORD: "admin123"

---
apiVersion: v1
kind: Secret
metadata:
  name: stripe-secrets
  namespace: oms
  labels:
    app.kubernetes.io/name: stripe
    app.kubernetes.io/part-of: oms
    environment: local-k3d
type: Opaque
stringData:
  # Stripe Test Keys - https://dashboard.stripe.com/test/apikeys
  STRIPE_SECRET_KEY: "sk_test_REPLACE_WITH_TEST_KEY"
  STRIPE_ENDPOINT_SECRET: "whsec_REPLACE_WITH_TEST_WEBHOOK_SECRET"
