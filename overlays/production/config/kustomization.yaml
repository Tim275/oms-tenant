apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Configuration management for production environment
# Enterprise Pattern: SealedSecrets für sensitive Daten
#
# NOTE: stripe-secrets SealedSecret ist in base/secrets/ definiert

resources:
- orders-mongodb-secret.yaml

# Note: Patches sind in parent kustomization.yaml definiert
