# OMS Secrets TEMPLATE - DO NOT DEPLOY DIRECTLY
# ==============================================
# Diese Datei ist NUR ein Template/Cheatsheet!
# Echte Secrets werden als SealedSecrets deployed.
#
# WORKFLOW:
# 1. Diese Datei mit echten Werten ausfüllen (lokal, nicht committen!)
# 2. Mit kubeseal verschlüsseln:
#    kubeseal --controller-name=sealed-secrets-controller \
#             --controller-namespace=sealed-secrets \
#             --format yaml < secrets.yaml > sealed-secrets.yaml
# 3. Die sealed-secrets.yaml committen
#
# OPERATOR-MANAGED SECRETS (NICHT hier definieren!):
# - rabbitmq-default-user → RabbitMQ Cluster Operator
# - postgres-app-user    → CloudNativePG Operator
# - mongodb-oms-secrets  → Percona MongoDB Operator
# ==============================================

---
# TEMPLATE: Stripe API Secrets
# Verschlüsselt in: sealed-stripe-secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: stripe-secrets
  namespace: oms
type: Opaque
stringData:
  STRIPE_SECRET_KEY: "sk_test_REPLACE_ME"
  STRIPE_ENDPOINT_SECRET: "whsec_REPLACE_ME"
